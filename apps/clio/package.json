{
  "name": "clio-service",
  "version": "0.1.0",
  "scripts": {
    "dev": "../../scripts/dev/run clio serve --host 127.0.0.1 --port 8001",
    "dev:worker": "../../scripts/dev/run clio worker"
  }
}
