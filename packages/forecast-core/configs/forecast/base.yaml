model:
  embed_dim: 1280
  solar_proj_dim: 256
  history_input_dim: 5
  history_hidden_dim: 128
  history_proj_dim: 128
  context_input_dim: 7
  context_proj_dim: 64
  fusion_dim: 256
  lead_dim: 16
  head_hidden_dim: 128
  depth: 2
  num_heads: 4
  mlp_ratio: 4.0
  dropout: 0.1
  output_steps: 96
  output_features: 4
  num_regimes: 4

inference:
  regime_names: [quiet, recurrent, transient, mixed]
