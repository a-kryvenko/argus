# Atmospheric density API

The API serves precomputed atmospheric density products from the configured
forecast service. Generation requires the separately installed private backend.
See the running API's `/docs` for request and response contracts.
Implementation, calibration and training documentation belongs to the private
backend repository.
