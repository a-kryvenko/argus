[project]
name = "forecast"
version = "0.1.0"
dependencies = [
  "numpy>=1.26",
  "pandas>=2.2",
  "pyyaml>=6.0",
  "scikit-learn>=1.5",
  "lightgbm>=4.6.0",
  "joblib>=1.5.3",
  "pyarrow>=24.0.0",
  "scipy>=1.13",
  "common",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.uv.sources]
common = { workspace = true, editable = true }
