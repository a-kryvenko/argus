[project]
name = "forecast"
version = "0.1.0"
dependencies = [
  "numpy>=1.26",
  "pandas>=2.2",
  "pyyaml>=6.0",
  "scikit-learn>=1.5",
  "cdflib>=1.1.0",
  "lightgbm>=4.6.0",
  "joblib>=1.5.3",
  "pyarrow>=24.0.0",
  "scipy>=1.13",
  "clio",
  "common",
  "pyatmos>=1.2.7",
  "astropy>=7.2.0",
  "setuptools<81",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.uv.sources]
clio = { workspace = true, editable = true }
common = { workspace = true, editable = true }
